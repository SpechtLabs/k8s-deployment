apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: tempo-vulture
    repo: https://grafana.github.io/helm-charts
    version: 0.10.1
    releaseName: grafana-vulture
    namespace: observability
    includeCRDs: true
    valuesFile: helm-values.yaml
